import { ScreenShell } from '@/components/screen-shell';
import { ThemedText } from '@/components/themed-text';

export default function CompareScreen() {
  return (
    <ScreenShell title="Comparação" subtitle="Compare notas e ingredientes lado a lado.">
      <ThemedText>Disponível no MVP+ (fase 2).</ThemedText>
    </ScreenShell>
  );
}
